
const PokeTitle = ({ name }) => {
    return (
        <span className="poke-title squada-one-regular">{name}</span>
    );
}

export default PokeTitle;

// import './PokeCard.css';

// const PokeTitle = ({ name }) => {
//     return (
//         <span className="poke-title squada-one-regular">{name}</span>
//     )
// }

// export default PokeTitle;